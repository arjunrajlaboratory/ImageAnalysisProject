# Crop

**Type:** Annotation Worker  
**Interface Category:** Image Processing  

## Description

Crops images, allowing you to drop particular slices of data and crop to blobs or rectangles

## Interface Parameters

| Parameter | Type | Required | Default | Range / Options | Description |
|-----------|------|:--------:|---------|-----------------|-------------|
| **XY Range** | `text` |  | -- | -- | Enter the XY positions you want to retain (default is all) |
| **Z Range** | `text` |  | -- | -- | Enter the Z positions you want to retain (default is all) |
| **Time Range** | `text` |  | -- | -- | Enter the Time positions you want to retain (default is all) |
| **Crop Rectangle** | `tags` |  | -- | -- | Select tag of the crop rectangle. Will take the first rectangle or blob with the specified tag. |

## Files

| File | Description |
|------|-------------|
| `entrypoint.py` | Main worker logic -- `interface()` defines the UI, `compute()` runs the analysis |
| `Dockerfile` | Docker build configuration (x86_64 / production) |
| `environment.yml` | Conda environment dependencies |
| `tests/` | pytest test suite with `Dockerfile_Test` |

## Building

```bash
./build_workers.sh crop
```

## Testing

```bash
./build_workers.sh --build-and-run-tests crop
```

---
_This file is auto-generated by `generate_worker_docs.py`. To update, edit `entrypoint.py` and re-run the generator, or open a PR -- the Claude Code hook regenerates docs automatically._
