# Random Forest Classifier

**Type:** Property Worker  
**Category:** Blobs  
**Annotation Shape:** `polygon`  
**Interface Category:** Classification  

## Description

This tool uses a Random Forest Classifier to classify blobs.

## Interface Parameters

| Parameter | Type | Required | Default | Range / Options | Description |
|-----------|------|:--------:|---------|-----------------|-------------|
| **Buffer radius** | `number` |  | 10 | 0 - 200 pixels | The buffer to use to generate an annulus around the blobs. |
| **Include texture features** | `checkbox` |  | True | -- | Include Haralick and other texture features from Mahotas. |
| **Texture scaling** | `number` |  | 8 | 1 - 255 levels | Number of gray levels to use for texture feature calculation. Lower values are faster but less detailed. |

## Files

| File | Description |
|------|-------------|
| `entrypoint.py` | Main worker logic -- `interface()` defines the UI, `compute()` runs the analysis |
| `Dockerfile` | Docker build configuration (x86_64 / production) |
| `Dockerfile_M1` | ARM64 / Apple Silicon Docker configuration |
| `environment.yml` | Conda environment dependencies |
| `tests/` | pytest test suite with `Dockerfile_Test` |

## Building

```bash
./build_workers.sh blob_random_forest_classifier
```

## Testing

```bash
./build_workers.sh --build-and-run-tests blob_random_forest_classifier
```

---
_This file is auto-generated by `generate_worker_docs.py`. To update, edit `entrypoint.py` and re-run the generator, or open a PR -- the Claude Code hook regenerates docs automatically._
